# nichts ausschließen, das den Offline-Build betrifft:
# vendor/*.js und dist/index.html werden BEWUSST committet (reproduzierbar + sofort nutzbar).
*.pyc
__pycache__/
.DS_Store
*.xlsx
# Dev-Test-Harness: nur Abhängigkeiten nicht committen.
# test/samples/ (Referenz-PDFs + *.expected.json) wird BEWUSST committet:
# reproduzierbares Regressions-Harness, das überall ohne Download grün laufen muss.
node_modules/
package-lock.json
